<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <!--
 Required only if your app needs to access images or photos
     that other apps created.
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!--
 Required only if your app needs to access videos
         that other apps created.
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!--
 Required only if your app needs to access audio files
         that other apps created.
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!--
 If your app doesn't need to access media files that other apps created,
         set the "maxSdkVersion" attribute to "28" instead.
    -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:requestRawExternalStorageAccess="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RegistroCuentas"
        tools:targetApi="31">
        <activity
            android:name=".activitys.ReloadActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activitys.CltEditActivity"
            android:exported="false"
            android:theme="@style/Theme.RegistroCuentas.NoActionBar" />
        <activity
            android:name=".activitys.CltDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.RegistroCuentas.NoActionBar" />
        <activity
            android:name=".activitys.ImageActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="false"
            android:label="@string/title_activity_image"
            android:theme="@style/Theme.RegistroCuentas.Fullscreen" />
        <activity
            android:name=".activitys.PayEditActivity"
            android:exported="false"
            android:theme="@style/Theme.RegistroCuentas.NoActionBar" />
        <activity
            android:name=".activitys.AccEditActivity"
            android:exported="false"
            android:theme="@style/Theme.RegistroCuentas.NoActionBar" />
        <activity
            android:name=".activitys.AccDtailsActivity"
            android:exported="false"
            android:theme="@style/Theme.RegistroCuentas.NoActionBar" />
        <activity
            android:name=".activitys.PayDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.RegistroCuentas.NoActionBar" />
        <activity
            android:name=".activitys.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize|stateHidden">
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/path" />
        </provider>
    </application>

</manifest>